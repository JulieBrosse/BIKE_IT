<h1>All bikes available around ******* address ******* </h1>

<!--  <div class="container">
    <div id="map", style="width: 100%; height: 600px;" data-markers="<%= @markers.to_json %>">
  </div> -->

  <div class="row">
    <% @bikes.each do |bike| %>
      <div class="col-xs-3 col-md-4">
        <div class="card" style="background-image: url(<%= cl_image_path bike.picture %>);">
          <h2><%= link_to bike.title, bike_path(bike) %></h2>
        </div>
      </div>
    <% end %>
  </div>
</div>


