<div class="rented_bikes">
  <ul>
    <% @bikes.each do |bike| %>
      <li>
        <%= bike.description.truncate_words(20) %>
      </li>
      <li>
        <%= cl_image_tag(bike.picture, class: "rented_picture") %>
      </li>
    <% end %>
  </ul>
</div>
